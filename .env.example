# Sao chép file này thành .env rồi điền key của project Supabase MỚI của bạn
# Lấy ở: Supabase Dashboard -> Project Settings -> API

SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
# Dùng "service_role" key (chạy phía server nên an toàn) để bỏ qua RLS
SUPABASE_KEY=your-service-role-key

PORT=3000
